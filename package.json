{
  "name": "glycoview",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "go run ./cmd/glycoview",
    "build:ui": "cd frontend && npx tsc -b && npx vite build --outDir dist"
  }
}
